/**
 * This file auto-generated by Fern from our API Definition.
 */

import { PhylloConnectApi } from "../../..";
import * as core from "../../../core";
import * as serializers from "../..";

export const User: core.schemas.ObjectSchema<User.Raw, PhylloConnectApi.connect.User> = core.schemas
  .object({
    externalId: core.schemas.property(
      "external_id",
      core.schemas.lazy(() => serializers.connect.ExternalId)
    ),
    createdAt: core.schemas.property("created_at", core.schemas.date()),
    updatedAt: core.schemas.property("updated_at", core.schemas.date()),
    status: core.schemas.lazy(() => serializers.connect.Status),
  })
  .extend(core.schemas.lazyObject(() => serializers.connect.UserAttribute));

export declare namespace User {
  interface Raw extends serializers.connect.UserAttribute.Raw {
    external_id: serializers.connect.ExternalId.Raw;
    created_at: string;
    updated_at: string;
    status: serializers.connect.Status.Raw;
  }
}
